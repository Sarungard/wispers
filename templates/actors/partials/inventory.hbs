<div class="inventory-body items-list">
    {{#each inventorySections as |section|}}
    <div class="inventory-section">

        <div class="items-header flexrow">
            <div class="item-name">
                <h3>{{localize section.labelKey}}</h3>
            </div>
            <div class="item-prop inv-col-header">{{localize "CONSTANTS.Inventory.ColQty"}}</div>
            <div class="item-prop inv-col-header">{{localize "CONSTANTS.Inventory.ColWgt"}}</div>
            <div class="item-prop inv-col-header">{{localize "CONSTANTS.Inventory.ColPrice"}}</div>
            <div class="item-controls">
                {{#if ../editable}}
                <a data-action="createItem" data-type="{{section.type}}"
                   data-tooltip="{{localize 'CONSTANTS.Inventory.AddItem'}}">
                    <i class="fas fa-plus fa-fw"></i>
                </a>
                {{/if}}
            </div>
        </div>

        <ol class="item-list">
            {{#each section.items as |item|}}
            <li class="item flexrow" draggable="true" data-item-id="{{item.id}}">
                <div class="item-name flexrow">
                    <img class="item-image" src="{{item.img}}" alt="{{item.name}}" />
                    <h4>{{item.name}}</h4>
                </div>
                <div class="item-prop">{{item.system.quantity.value}}</div>
                <div class="item-prop">{{item.system.weight.value}}</div>
                <div class="item-prop">{{item.system.price.value}}</div>
                <div class="item-controls flexrow">
                    {{#if item.system.equipped}}
                        {{#if ../../editable}}
                        <a class="item-control equip-toggle{{#if item.system.equipped.value}} is-equipped{{/if}}"
                           data-action="toggleEquipped" data-item-id="{{item.id}}"
                           data-tooltip="{{#if item.system.equipped.value}}{{localize 'CONSTANTS.Inventory.Unequip'}}{{else}}{{localize 'CONSTANTS.Inventory.Equip'}}{{/if}}">
                            <i class="fas fa-shield-halved fa-fw"></i>
                        </a>
                        {{else}}
                        <i class="fas fa-shield-halved fa-fw item-control equip-toggle{{#if item.system.equipped.value}} is-equipped{{/if}}"></i>
                        {{/if}}
                    {{/if}}
                    {{#if ../../editable}}
                    <a class="item-control" data-action="editItem" data-item-id="{{item.id}}"
                       data-tooltip="{{localize 'CONSTANTS.Inventory.EditItem'}}">
                        <i class="fas fa-edit fa-fw"></i>
                    </a>
                    <a class="item-control" data-action="deleteItem" data-item-id="{{item.id}}"
                       data-tooltip="{{localize 'CONSTANTS.Inventory.DeleteItem'}}">
                        <i class="fas fa-trash fa-fw"></i>
                    </a>
                    {{/if}}
                </div>
            </li>
            {{/each}}
        </ol>

    </div>
    {{/each}}
</div>
